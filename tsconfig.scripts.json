{
  "// why": [
    "The scripts run in Node, not in a Worker: they read and write files and",
    "they are what maintains the gallery. The main tsconfig types them against",
    "@cloudflare/workers-types and does not include them at all, so until the",
    "21.09 audit nothing typechecked the code that rewrites data/gallery.json."
  ],
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node"],
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "allowImportingTsExtensions": true,
    "noEmit": true
  },
  "include": ["scripts"]
}
