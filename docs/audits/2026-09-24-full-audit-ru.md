**Bet or Book - аудит 24 сентября 2026 года (вечер, после закрытия аудита 23.09)**

Проверенная версия: `ff92691`, она же задеплоена. Проверены исходники Worker (маршруты, три стадии проверки, правила, признаки, нормализация и валидация источников, бюджет и лимиты, снимки и сравнение, og-картинка), страница и её скрипт, данные галереи и демонстрационных чтений, документация и живой сайт. Исходники, данные и деплой в ходе аудита не менялись; ни одной платной проверки не запускалось.

**Вывод.** Всё, что просил аудит 23.09, сделано и работает: три стадии, валидация каждого ответа upstream, честное сравнение чтений, картинка ссылки, операторский ключ в заголовке, четыре свежих демонстрационных чтения, runtime-тесты. Логика и безопасность в порядке. Что осталось - подача: первый экран без картинки, у карточек Book и Bet нет «своей» картинки и решающего числа, один и тот же счёт дважды на странице с разными цифрами, строка «Your recent checks» врёт. Это дешёвые правки, и именно они решают первые три секунды у судьи. По критериям конкурса это верхняя группа заявок; «сильным фаворитом» назвать нельзя без знания поля, а ролик, пост и форма ещё не сделаны.

**Что фактически проверено.**

- `npm test`: **551/551**, 42 файла. `npm run test:runtime`: **9/9** на workerd. `npm run typecheck`: обе конфигурации. `npm audit`: 0 уязвимостей.
- Живой сайт: `/`, `/app.js`, `/api/gallery` (110 КБ вместо прежних 730), `/api/ledger`, `/api/snapshot`, `/api/og` (PNG), `/?s=` с подставленными og-тегами - все 200. `GET /api/check` без записи - 404 и ничего не тратит. POST с чужим `Origin` - 403 до расхода. Мусорный адрес и мусорный id - 400. `/?s=несуществующий` - обычная страница.
- Заголовки: CSP без inline-script, `frame-ancestors 'none'`, `nosniff`, `referrer-policy`.
- В браузере открыты все четыре демонстрационных чтения: десктоп, 375 px, тёмная и светлая тема. Скачаны картинка canvas («Download image») и og-картинки всех четырёх чтений плюс заглушка.
- Сверены `data/featured.json` и `data/gallery.json`: версии правил, `superseded`, дубли адресов.

Не проверено: кабинеты Nansen и Cloudflare, реальная многорегиональная конкуренция, точность правил на независимой разметке, ролик, пост, форма.

**Логика и данные.**

**Л01 · P1 - Карточка Book не показывает число, на котором стоит вердикт, а её картинка спорит с бейджем.** Правила v5 признают книгу по котировкам в рынке самой позиции: у демонстрационного счёта `headlineTwoSidedNotionalUsd` = $42,9M против $46,9M шорта (91%). На карточке этого числа нет нигде: summary «2,613 resting orders quote both sides of 123 markets», тайл Resting orders - счёт и число рынков. README этим числом гордится, карточка молчит. Хуже: диаграмма для Book рисует хедж - «$45,9M (98%) not checked for a hedge» под бейджем Book. Хедж для книги не искали по правилу (`hedgeCanChangeVerdict` даёт false), а картинка показывает пропуск как дырку в чтении. `docs/demo-script.md` сам признаёт, что полоска книги «говорит меньше предложения», и выкидывает Book-бит из ролика. Место: `src/engine/evidence.ts` (`bookSummary`, тайлы Book), `src/engine/breakdown.ts`. Исправление: для Book полоска = котировки в рынке позиции (matched $42,9M из $46,9M), решающий тайл «Two-sided quoting in ETH: $42.9M (91% of the position)», в summary добавить «including $42.9M matched in ETH itself». Тогда Book-бит можно вернуть в ролик.

**Л02 · P1 - У самого частого ответа нет ни решающего числа, ни картинки.** «Looks like a bet» - 132 из 191 текущих карточек. Решающий тайл на нём - «Largest position $130.0M HYPE long»: тавтология, потому что `DECISIVE_LABEL_BY_REASON` не знает `directional_concentration` и `markDecisive` берёт первый элемент. Диаграммы для лонга нет вовсе, og-картинка - пустая нижняя половина (проверено на `3p2dqa2fjl01r`). Исправление: один визуальный язык на все четыре ответа - bet = полоска экспозиции (gross по позициям, главная выделена; одна позиция = 100%), hedged = покрытие, book = котировки, unknown с фондировщиками = покрытие плюс пунктирный блок. Решающий тайл для bet - «Share of exposure 100%, no two-sided quotes, no cover found».

**Л03 · P2 - Один счёт показан дважды с разными цифрами.** Чип «$209.1M ETH short» и строка галереи #2 «$216.7M ETH short · 0xb83d...6e36» - один адрес; чип «$130.0M HYPE long» и строка #3 «$125.9M HYPE long»; чип «$41.8M HYPE short» и строка #15 «$42.7M HYPE short». `galleryIndex` убирает только записи с `superseded` внутри `gallery.json`; демонстрационные чтения ссылаются на галерейные через `supersedes`, но обратной пометки в галерее нет. Проверено по данным: `07dr4tcn26rwx`, `082e843a-mu71i2vu`, `1ny4ncx4j6myd` - `superseded=false`. Исправление: в `listedGallery` (`src/index.ts`) не показывать строку, для адреса которой есть более новое featured-чтение, либо подписывать её «re-read 24 Sept».

**Л04 · P2 - «Your recent checks» наполняется чтениями, которые посетитель просто открыл.** `saveRecent` вызывается в `renderResult` для любого `kind`, включая чипы и галерею. Проверено вживую: после трёх чипов строка показывает три «recent checks», хотя проверок не было. Либо «Recently opened», либо сохранять только live.

**Л05 · P2 - Вклад Nansen для Book и Bet звучит слабо, хотя доказательство лежит рядом бесплатно.** Строка «From Nansen» на книге: «positions on every dex, 134 open positions». README при этом даёт контраст «134 против 86 на main dex», которого карточка не знает: `clearinghouseState` читается только как fallback. Бесплатный запрос уже есть; читать его всегда параллельно с остальными бесплатными и печатать дельту: «134 positions on Nansen; 86 visible on Hyperliquid's main dex». Ноль кредитов, прямое доказательство Data Integration на каждой карточке, плюс перекрёстная проверка размеров позиций между источниками.

**Л06 · P2 - Дубль тайла.** «Size vs open interest» стоит и в evidence, и в vitals на одной карточке (демонстрационный bet). Он ничего не решает - место ему в vitals. Туда же логично перенести «Realized PnL, 30d»: аудит 23.09 просил не делать PnL счёта уликой о позиции, а он всё ещё среди evidence.

**Л07 · P3 - Отзыв bet при любом двустороннем рынке без порога материальности.** `concentrated` и `directional` требуют `coinsBothSides === 0`, а `coinsBothSides` считает рынок двусторонним при bid > 0 и ask > 0 любого размера. Book с 23.09 требует материальности (2 × min(bid, ask) ≥ $10K и 10% позиции), отзыв bet - нет: $200 двусторонних заявок в мемкоине снимают ответ по $20M HYPE long (кейс 13 в `docs/case-review.md`; 21 «signals disagree» из 191). Логично применить тот же порог. Но это смена правил (v6) с перепрочтением галереи за три дня до дедлайна - отложить после подачи, если нет дня запаса.

**Л08 · P3 - Ключ кэша `check:` не содержит `ASSET_REGISTRY_VERSION` и `OBSERVATION_SCHEMA_VERSION`.** После деплоя с новым реестром до десяти минут отдаются ответы старого. Мелочь.

**Л09 · P3 - Мёртвый код.** `looksLikeButUnverified` (`src/engine/assets.ts`) и действие `sync` бюджета (`src/budget.ts`, `src/coordinator.ts`) никем не вызываются. Отмечалось 22.09 (K12), осталось.

**Л10 · P3 - Словарь причин на фронте.** `headlineFor` в `web/app.js` держит семь `reasons.indexOf(...)` с собственными заголовками, хотя `rule`, `openQuestion` и `nansen` уже приходят с сервера из `explained()`. Перенести заголовок туда же - одна точка правды.

**Интерфейс, путь пользователя, визуал.**

**U01 · P1 - Первый экран текстовый, а сильнейший визуал в одном клике.** Заголовок, подзаголовок, поле, четыре чипа, список галереи. Полоска с пунктирным блоком «held elsewhere» - то, что судья должен увидеть за три секунды - появляется только после клика по чипу. Исправление: при заходе без `?s=` и без `?address=` сразу открывать флагманское чтение (фондировщики) как «Saved reading from 24 Sept» - оно бесплатное, бандлится с Worker и уже честно подписано. Одна строка в конце `web/app.js`. Сначала закрыть Л04, иначе автооткрытие попадёт в «recent checks».

**U02 · P2 - Бейдж «Unknown» на флагмане и на 21% галереи.** Заголовок объясняет, но бейдж - то, что видно в три секунды, на чипах «recent» и в og-заголовке ссылки («ETH short: not settled by what could be read»). Внутренние четыре вердикта оставить; бейджу для unknown дать состояние по причине: «Unknown · assets sit with funders», «Unknown · hedge not checked», «Unknown · partly covered». Словарь уже есть в `src/engine/reasons.ts`.

**U03 · P2 - Ожидание с одной статичной строкой.** Проверка идёт 5-10 секунд, на экране одна фраза. Без стриминга честный вариант: показать план чтения списком (Hyperliquid: orders, fills, spot balances; Nansen: positions on every dex, balances on every chain, funding links) с индикатором и подписью «usually 5-10 seconds», ничего не отмечая галочками - клиент не знает, где сервер.

**U04 · P2 - Название.** На странице «Bet or book», в README, og и заголовке вкладки «Bet or Book». Первое слово на экране должно совпадать с брендом.

**U05 · P2 - og-картинки.** Для шорта - отличные (бейдж, предложение, полоска, пунктирный блок, дата, лимит). Для лонга - пустая половина, для Book - почти пустая полоска «хеджа» (см. Л01, Л02). После них решится само. Заглушка «Paste a Hyperliquid address…» - хороша.

**U06 · P3 - Мобильная версия и светлая тема.** 375 px проверено: карточка читается, диаграмма в стопку работает, подписи 12 px. Светлая тема работает. До карточки на телефоне около 1000 px чипов и подписей; при автооткрытии (U01) чипы можно ужать в одну прокручиваемую строку.

**U07 · P3 - Галерея под карточкой.** 177 строк, 132 bet, почти все лонги: честно, но для судьи малоинформативно. Свернуть в `<details>` «Browse the 177 scanned positions» по умолчанию, чтобы страница заканчивалась карточкой и футером.

**U08 · P3 - Тексты.** «This looks like a real bet.» - слово real спорит с «looks like»; лучше «This looks like a directional bet.» «What changed since…» на книге: «went from Book (strong) to Book (likely) because both the reading and the rules changed» - верно и хорошо.

**Безопасность и эксплуатация.**

**S01 · P2 - Публичный дневной бюджет исчерпывается за минуты, а судьи будут нажимать Check всю неделю.** Публично доступно 260 из 300 кредитов; проверка стоит 2-7. Curl-POST без `Origin` проходит `fromThisSite` осознанно. 40-130 проверок - и до полуночи UTC любой судья получает «Nansen not used (today's credits are used up)». Резерв 40 защищает ролик, не судей. На 25-30 сентября поднять `NANSEN_DAILY_CREDIT_CAP` (баланс 916 кредитов), резерв оставить; после итогов вернуть. Turnstile - только если будет замечено злоупотребление.

**S02 - Остальное по чек-листу запуска не изменилось с 23.09 и в порядке:** логинов, сессий, SQL, вебхуков и загрузок нет; единственный ввод - адрес, монета, сторона, id снимка, все проверяются регулярками до попадания в путь или ключ; два фиксированных upstream-хоста; ключ в секрете Worker и git-ignored `.dev.vars`, CI ищет утечку по шаблону; платное - только POST с этого сайта; бюджет и лимиты в Durable Objects; операторский ключ в заголовке, сравнение по SHA-256; ошибки клиенту общие; `npm audit` 0.

**Документация и репозиторий.**

**D01 · P2 - README устарел в цифрах уже сегодня.** 1 136 вызовов против 1 173 в живом `/api/ledger`; 548 тестов против 551. Перед подачей обновить или заменить фиксированные числа ссылкой на ledger.

**D02 · P2 - Внутренние стратегические документы в публичном репозитории.** `2026-09-19-pivot-ideas-ru.md`, `2026-09-19-useful-ideas-ru.md`, `2026-09-20-retail-actions-ru.md`, `2026-09-20-competition-decision-ru.md` - на русском, о том, не бросить ли продукт ради детективной игры или условных ордеров. Судье это шум и сбивающий сигнал. Перенести в `docs/internal/` или убрать из дерева (история останется в git). Аудиты 19-23.09 с воспроизведениями - наоборот, актив: оставить.

**D03 · P3 - Рабочие остатки в `data/`.** `rescan-offset.json`, `rescan-offset2.json`, `rescan-offset3.json`, `prescan-candidates.json` (6 054 строки) не описаны в README (22.09, C04). Описать одной строкой или убрать.

**D04 · P3 - Плотность комментариев.** История аудитов в комментариях объясняет «почему», для хакатона это скорее плюс. После подачи - вынести историю в ADR, оставить инварианты.

**Что лишнее или неактуальное.**

- Внутренние документы о пивоте (D02).
- Файлы `rescan-offset*` (D03).
- Дубль «Size vs open interest» и PnL среди улик (Л06).
- Мёртвые функции (Л09).
- Строка «Your recent checks» в нынешнем виде (Л04).
- Строка галереи для счёта, у которого есть свежее демонстрационное чтение (Л03).

**Механики до подачи, по цене и эффекту.**

| # | Что | Закрывает | Оценка |
|---|---|---|---|
| 1 | Полоска котировок и решающий тайл для Book | Л01, U05 | 2-3 ч |
| 2 | Полоска экспозиции для лонга и портфельного bet, включая og | Л02, U05 | 2-3 ч |
| 3 | Дедуп чипов и галереи | Л03 | 30 мин |
| 4 | «Recently opened» или только live | Л04 | 15 мин |
| 5 | Всегда читать main dex бесплатно, печатать дельту с Nansen | Л05 | 1 ч |
| 6 | Автооткрытие флагмана при пустом заходе | U01 | 30 мин |
| 7 | Бейдж по причине для Unknown | U02 | 1 ч |
| 8 | Поднять cap на неделю судейства | S01 | 5 мин и деплой |
| 9 | README-цифры, перенос внутренних документов | D01, D02 | 30 мин |

После подачи: порог материальности для отзыва bet (Л07, v6), watchlist в браузере, наблюдение котировок во времени, Turnstile. Не добавлять: торговлю, копитрейд, AI-чат, скоринг 0-100, Telegram-бота, новые сети - они не закрывают найденное и размывают вопрос.

**Оценка по критериям конкурса.** Субъективная, не прогноз судей.

| Критерий | 23.09 | Сейчас | Что даст рост |
|---|---|---|---|
| Data Integration | 7 | 8 | Л05 и Л01: вклад Nansen виден на каждой карточке, не только на флагмане |
| Originality | 8 | 8,5 | вопрос, отказ от вывода, пунктирный блок «held elsewhere» |
| Functionality | 6,5 | 8 | после Л01-Л04 - 8,5 |
| Documentation & Submission | 7 | 7 | ролик, пост и форма не сделаны; после них - 8,5 |

Главные риски заявки: ролик и подача ещё не сделаны; первые три секунды на сайте - текст; исчерпание публичного бюджета в неделю судейства. Ни один из них не про логику и не про безопасность.

**Порядок работ.**

1. Л04, Л03, U04 - полчаса, убирают видимые несоответствия.
2. Л01 и Л02 - один визуальный язык на четыре ответа; после этого перерисовать og бандла (`scripts/prerender-og.ts --upload`, это новый layout v4 и новый ключ).
3. Л05, U01, U02 - вклад Nansen и первый экран.
4. S01, D01, D02 - настройка и репозиторий.
5. Ролик по `docs/demo-script.md` с возвращённым Book-битом, пост, форма, сверка счётчика на стороне Nansen.

**Приложение. Разбор внешнего маркетингового отзыва (получен 24.09 вечером).**

Диагноз отзыва верный и совпадает с U01, U02, Л01, Л02: подача суше продукта, первый экран без картинки, у Book и Bet нет своей визуализации. Лекарства в основном не подходят: они меняют метафору вместо иерархии и требуют рисовать каждую картинку дважды (SVG на странице и satori для картинки ссылки), что уже один раз привело к расхождению страницы и превью (U02 от 23.09). Общий критерий отбора: берём то, что делает существующие цифры заметнее; отклоняем то, что заменяет цифры метафорой или добавляет оценку («безрассудный», «Debunked»), которую данные не поддерживают.

| Идея из отзыва | Решение | Почему |
|---|---|---|
| Слой «и что с того» после вердикта | Взять как формулировку заголовка, без совета | Заголовок карточки уже есть; сделать его ответом на вопрос читателя: «Not a view on price: a market maker's inventory», «One directional view, nothing found against it». Совет «не ориентируйтесь» продукт не даёт сознательно |
| Угадать исход до показа | Взять (решение пользователя 24.09) для живого ожидания | Слот честный: 5-10 с ожидания пустые, ответ не задерживается. Только на живой проверке и только если ответ не пришёл за ~700 мс; четыре чипа Bet / Hedge / Book / Can't tell, необязательно; после ответа одна строка «You guessed X. The reading says Y», счётчик совпадений в localStorage. Слово «правильно» не писать. Готовый бит для ролика на 4-8 секунде |
| Доска рейтингов | Взять (решение пользователя 24.09), в измеримой форме и без слова «безрассудный» | Ранжировать наблюдаемое: bet по размеру, доля открытого интереса рынка, шорты с покрытием ниже 10% и в 85-115%, книги по котировкам; у каждой строки дата чтения. Строки: 191 текущих чтений плюс 4 сегодняшних уже дают доску; динамика - ежедневное перечитывание 10-15 верхних адресов через prescan (40-50 кредитов в день) или живые проверки посетителей. В интерфейсе не писать «leaderboard»; перед выкладкой перечитать гайд Nansen по редистрибуции: ранжируем свои вычисленные признаки из profiler-эндпоинтов, не данные perp-leaderboard. Заменяет список «More readings» (закрывает U07) |
| Карточка как «трофей» или «разоблачение» | Иерархию усилить, слова оставить | Крупнее бейдж и полоска, меньше серого текста - да. «Debunked» - нет: лучшая карточка проекта говорит «Unknown: the ETH sits with its funders», и это не разоблачение |
| Гравитационная модель, планеты и орбиты | Отклонить | Орбита не кодирует величину; полоска отвечает на вопрос «какая доля покрыта» за секунду, орбита - нет. Плюс двойная отрисовка для превью |
| Сканирующий луч во время загрузки | Взять нейтральную версию | Честная анимация ожидания решает U03. Показывать «шестерёнки маркетмейкера» до вердикта нельзя: вердикта ещё нет |
| Заливка экрана цветом вердикта | Отклонить; усилить существующий акцент | Красный для bet - редакционная оценка (ставка не «плохая»); цвет как единственный носитель смысла плох для доступности. Четыре приглушённых цвета уже есть; допустимо вынести их на рамку карточки |
| «Анти-FUD патруль», шаблоны ответов под твиты | Взять как позиционирование | Точка входа продукта - ответ под постом о ките (J01 в аудите 22.09). Кнопка «Copy post text» уже делает это; переименовать в «Copy reply for X» и добавить заголовок. Без слова Debunked |
| Embed-виджет для СМИ | После подачи | CSP `frame-ancestors 'none'` стоит намеренно. Встраивать можно только сохранённые чтения по отдельному маршруту, без кнопки Check: иначе чужая страница тратит наши кредиты по клику читателя. До дедлайна никто не встроит |
| «Досье», расшифровка деталей на глазах | Отклонить | Детали спрятаны по просьбе аудита, чтобы не шуметь; театр задерживает чтение и раздражает того, кто пришёл проверить цифру |
| Весы риска | Взять (решение пользователя 24.09: полоска признана непонятной первым же читателем) | Левая чаша - позиция, правая - только засчитанное покрытие, наклон балки = отношение (0% максимальный наклон, 85-115% ровно, выше - перевес с подписью «leans long»). Незасчитанное не лежит на чаше: ETH фондировщиков висит рядом на пунктирной нити «not on the scale: funding is not ownership», нераспознанные токены там же штрихом, «не проверяли» - призрачная чаша с вопросом. Book - не весы, а мини-стакан: биды слева, аски справа, рынок позиции выделен, подпись «$42.9M quoted both sides of ETH itself». Лонг: правая чаша пуста по определению, подпись обязана говорить «spot cannot offset a long», иначе читается как «искали и не нашли». Три отрисовки (SVG, canvas, satori) и новый layout превью v4 с перезаливкой 310 картинок - день работы; сначала SVG страницы и показ двум-трём людям рядом с полоской, если к вечеру 26.09 не готово - в ролик идёт полоска |
| Детективная доска связей | Уже есть в минимальной форме | Пунктирная связь к блоку «held elsewhere» и есть граф из двух узлов; фондировщиков читается не больше двух (`MAX_FUNDERS`) |
| Эхо-радар для Book | Возможно, как вторая строка под полоской, после Л01 | Единственная идея, закрывающая реальную дыру (у Book нет картинки). Но героем должна остаться цифра котировок в рынке позиции ($42,9M из $46,9M); точки по 123 рынкам - украшение под ней |
| Telegram-бот | После подачи | Вне объёма по спеке и аудиту 23.09. Бот - это платная проверка по одной команде без сайта: публичный бюджет уйдёт за час. Технически тонкий клиент над `/api/check` и `/api/og` пишется за день, но нужны свой резерв и лимит на пользователя, и он не двигает ни один критерий судей |

**Решения пользователя 24.09 вечером и порядок с ними.** Три механики из отзыва приняты: угадывание во время живого ожидания (1-2 ч, только app.js), доска рейтингов по измеримым признакам (3-4 ч плюс 1 ч на ежедневное перечитывание верхних адресов), весы вместо полоски для шортов с мини-стаканом для книг (день работы, три отрисовки). Л01 и Л02 поглощаются переходом на весы: у книги появляется своя картинка, у лонга - честная пустая чаша с подписью. Порядок: 25.09 - Л03, Л04, угадывание, доска; 26.09 - весы (SVG, затем canvas и satori), перерисовка og бандла, финальная проверка страницы на 375 px; 27.09 - ролик, пост, форма, сверка счётчика на стороне Nansen. Правила (v5) не трогать.
